import numpy as np
import matplotlib.pyplot as pl

'''
x=np.arange(0,5*np.pi,0.1)
y=np.sin(x)
z=np.cos(x)

#pl.plot(x,y)
pl.plot(x,z)

pl.show()
'''

a = np.array([-1, 0, 1, 2, 4, 6, 8])
print(np.exp(a))
